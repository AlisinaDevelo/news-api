apiVersion: v1
kind: Service
metadata:
  name: news-api
  labels:
    app: news-api
spec:
  type: ClusterIP
  selector:
    app: news-api
  ports:
    - name: http
      port: 80
      targetPort: http
